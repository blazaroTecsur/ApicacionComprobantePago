<div class="panel-contenedor">

    <!-- Header -->
    <div class="panel-principal">
        <h5><i class="bi bi-receipt"></i> Consulta de Comprobantes de Pago</h5>
    </div>

    <!-- Filtros -->
    <div class="panel-filtros">
        <div class="row g-2 align-items-end">
            <div class="col-md-2">
                <label class="form-label">Tipo Comprobante</label>
                <select class="form-select form-select-sm" id="index_cboTipo">
                    <option value="">-- Todos --</option>
                </select>
            </div>
            <div class="col-md-2">
                <label class="form-label">Estado</label>
                <select class="form-select form-select-sm" id="index_cboEstado">
                    <option value="">-- Todos --</option>
                </select>
            </div>
            <div class="col-md-3">
                <label class="form-label">Proveedor</label>
                <input type="text" class="form-control form-control-sm"
                       id="index_txtProveedor" placeholder="RUC o razón social">
            </div>
            <div class="col-md-2">
                <label class="form-label">Folio</label>
                <div class="input-group input-group-sm">
                    <input type="text" class="form-control" id="index_txtFolio">
                    <button type="button" class="btn btn-success" id="index_btnBuscar">
                        <i class="bi bi-search"></i>
                    </button>
                </div>
            </div>
            <div class="col-md-3 d-flex align-items-end gap-2">
                <button type="button" class="btn btn-primary btn-sm"
                        id="index_btnNuevo">
                    <i class="bi bi-file-earmark-plus"></i> Nuevo
                </button>
                <button type="button" class="btn btn-success btn-sm"
                        id="index_btnExportarCabecera">
                    <i class="bi bi-file-excel"></i> Exportar Cabecera Syteline
                </button>
                <button type="button" class="btn btn-success btn-sm"
                        id="index_btnExportarImputacion">
                    <i class="bi bi-file-excel"></i> Imputación
                </button>
            </div>
        </div>
    </div>

    <!-- Tabla -->
    <div class="panel-tabla">
        <table id="index_tblComprobantes"
               class="table tabla-corporativa w-100">
            <thead>
                <tr>
                    <!-- ← agregar columna checkbox -->
                    <th>
                        <input type="checkbox" id="chkTodos"
                               title="Seleccionar todos" />
                    </th>
                    <th>FOLIO</th>
                    <th>TIPO</th>
                    <th>SERIE</th>
                    <th>NÚMERO</th>
                    <th>PROVEEDOR</th>
                    <th>FECHA</th>
                    <th>MONEDA</th>
                    <th>MONTO TOTAL</th>
                    <th>ESTADO</th>
                    <th>ACCIONES</th>
                </tr>
            </thead>
        </table>
    </div>

</div>

<div id="modalContainer"></div>

@section Scripts {
    <script src="~/js/Comprobante/index.js" asp-append-version="true"></script>
}